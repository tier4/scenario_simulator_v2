name: Docker
on:
  pull_request:
    paths:
      - "**"
      - "!docs/**"
      - "!README.md"
      - "!CONTRIBUTING.md"
      - "!.github/**"
      - ".github/workflows/Docker.yaml"
      - "!mkdocs.yml"
      - "!pyproject.toml"
      - "!sonar-project.properties"
      - "!uv.lock"
  push:
    branches:
      - master
    paths:
      - "**/package.xml"
  workflow_dispatch:
    inputs:
      version:
        description: version of the scenario_simulator_v2
        required: true

env:
  IMAGE_BASE: ghcr.io/tier4/scenario_simulator_v2
jobs:
  build:
    name: Build Docker Image
    runs-on: ${{ matrix.runs-on }}
    timeout-minutes: 720
    strategy:
      matrix:
        include:
          - runs-on: ubuntu-22.04
            rosdistro: humble
            platform: amd64
            dockerfile: Dockerfile
            image_tag: humble
          - runs-on: ubuntu-22.04-arm
            rosdistro: humble
            platform: arm64
            dockerfile: Dockerfile
            image_tag: humble
          - runs-on: ubuntu-24.04
            rosdistro: jazzy
            platform: amd64
            dockerfile: Dockerfile
            image_tag: jazzy
          - runs-on: ubuntu-24.04-arm
            rosdistro: jazzy
            platform: arm64
            dockerfile: Dockerfile
            image_tag: jazzy

    steps:
      - name: Free Disk Space (Ubuntu)
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: false

      - name: Install catkin-pkg
        run: sudo apt update && sudo apt install -y python3-catkin-pkg git curl

      - name: Install docker for ubuntu runner
        uses: docker/setup-buildx-action@v4

      - uses: actions/checkout@v6

      - name: Get version
        id: version
        run: echo "version=$(catkin_package_version)" >> $GITHUB_OUTPUT

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_BASE }}

      - name: Build development stage
        id: build_development
        uses: docker/build-push-action@v7
        with:
          context: .
          push: ${{ github.event_name == 'push' || github.event_name == 'workflow_dispatch' }}
          platforms: linux/${{ matrix.platform }}
          labels: ${{ steps.meta.outputs.labels }}
          file: ${{ matrix.dockerfile }}
          outputs: type=image,name=${{ env.IMAGE_BASE }},push-by-digest=${{ github.event_name == 'push' || github.event_name == 'workflow_dispatch' }},name-canonical=true,push=${{ github.event_name == 'push' || github.event_name == 'workflow_dispatch' }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          target: development
          build-args: |
            ROS_DISTRO=${{ matrix.rosdistro }}

      - name: Build desktop stage
        id: build_desktop
        uses: docker/build-push-action@v7
        with:
          context: .
          push: ${{ github.event_name == 'push' || github.event_name == 'workflow_dispatch' }}
          platforms: linux/${{ matrix.platform }}
          labels: ${{ steps.meta.outputs.labels }}
          file: ${{ matrix.dockerfile }}
          outputs: type=image,name=${{ env.IMAGE_BASE }},push-by-digest=${{ github.event_name == 'push' || github.event_name == 'workflow_dispatch' }},name-canonical=true,push=${{ github.event_name == 'push' || github.event_name == 'workflow_dispatch' }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            ROS_DISTRO=${{ matrix.rosdistro }}
          target: desktop

      - name: Build runtime stage
        id: build_runtime
        uses: docker/build-push-action@v7
        with:
          context: .
          push: ${{ github.event_name == 'push' || github.event_name == 'workflow_dispatch' }}
          platforms: linux/${{ matrix.platform }}
          labels: ${{ steps.meta.outputs.labels }}
          file: ${{ matrix.dockerfile }}
          outputs: type=image,name=${{ env.IMAGE_BASE }},push-by-digest=${{ github.event_name == 'push' || github.event_name == 'workflow_dispatch' }},name-canonical=true,push=${{ github.event_name == 'push' || github.event_name == 'workflow_dispatch' }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            ROS_DISTRO=${{ matrix.rosdistro }}
          target: runtime

      - name: Export digest
        run: |
          mkdir -p /tmp/desktop/digests
          digest="${{ steps.build_desktop.outputs.digest }}"
          touch "/tmp/desktop/digests/${digest#sha256:}"

          mkdir -p /tmp/development/digests
          digest="${{ steps.build_development.outputs.digest }}"
          touch "/tmp/development/digests/${digest#sha256:}"

          mkdir -p /tmp/runtime/digests
          digest="${{ steps.build_runtime.outputs.digest }}"
          touch "/tmp/runtime/digests/${digest#sha256:}"

      - name: Upload digest (desktop)
        uses: actions/upload-artifact@v7
        with:
          name: digests-desktop-${{ matrix.image_tag }}-${{ matrix.platform }}
          path: /tmp/desktop/digests/*
          if-no-files-found: error
          retention-days: 1

      - name: Upload digest (development)
        uses: actions/upload-artifact@v7
        with:
          name: digests-development-${{ matrix.image_tag }}-${{ matrix.platform }}
          path: /tmp/development/digests/*
          if-no-files-found: error
          retention-days: 1

      - name: Upload digest (runtime)
        uses: actions/upload-artifact@v7
        with:
          name: digests-runtime-${{ matrix.image_tag }}-${{ matrix.platform }}
          path: /tmp/runtime/digests/*
          if-no-files-found: error
          retention-days: 1

  merge:
    runs-on: ubuntu-latest
    needs:
      - build
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    strategy:
      matrix:
        include:
          - image_tag: humble
            variant: desktop
          - image_tag: humble
            variant: development
            image_suffix: -devel
          - image_tag: humble
            variant: runtime
            image_suffix: -runtime
          - image_tag: jazzy
            variant: desktop
          - image_tag: jazzy
            variant: development
            image_suffix: -devel
          - image_tag: jazzy
            variant: runtime
            image_suffix: -runtime
    steps:
      - name: Download digests
        uses: actions/download-artifact@v7
        with:
          path: /tmp/digests
          pattern: digests-${{ matrix.variant }}-${{ matrix.image_tag }}-*
          merge-multiple: true

      - name: Install catkin-pkg
        run: sudo apt update && sudo apt install -y python3-catkin-pkg git curl

      - uses: actions/checkout@v6

      - name: Get version
        id: version
        run: echo "version=$(catkin_package_version)" >> $GITHUB_OUTPUT
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Create image tag
        id: image_tag
        run: echo "image_tag=${{ env.IMAGE_BASE }}:${{ matrix.image_tag }}-${{ github.event.inputs.version || steps.version.outputs.version }}${{ matrix.image_suffix }}" >> $GITHUB_OUTPUT

      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Create manifest list and push
        working-directory: /tmp/digests
        run: |
          docker buildx imagetools create -t ${{ steps.image_tag.outputs.image_tag }} $(printf '${{ steps.image_tag.outputs.image_tag }}@sha256:%s ' *)

      - name: Add latest tag
        if: github.ref == 'refs/heads/master' && matrix.image_suffix == 'desktop'
        run: |
          docker buildx imagetools create -t ${{ env.IMAGE_BASE }}:${{ matrix.image_tag }}-latest ${{ steps.image_tag.outputs.image_tag }}
